# SDD Configuration
# Generated by `sdd init` or `sdd config init`.
# This file controls the SDD framework runtime behavior.

neo4j:
  uri: bolt://172.20.0.2:7687
  username: neo4j
  password: password
  database: neo4j
specs_dir: ./specs
default_branch: main
rules:
  disabled: []
semantic:
  # Judge model for the advisory semantic lane (frozen per ADR-052).
  # API key comes from the environment (OPENAI_API_KEY), never from this file.
  model:
    provider: openai
    model_id: claude-haiku-4-5-20251001
    base_url: http://127.0.0.1:54001/v1
  # FR82 FAIL-class precision floor (explicit pin of the governed 0.8 default).
  # Raising it shrinks strict escalation eligibility; lowering it widens it.
  precision_floor: 0.8
