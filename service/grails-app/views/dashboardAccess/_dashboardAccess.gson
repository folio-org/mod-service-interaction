import  org.olf.DashboardAccess

import groovy.transform.*

@Field
DashboardAccess dashboardAccess

def expand = ['access']

if (controllerName == 'dashboard' && actionName != 'getDashboardUsers') {
  expand << 'dashboard'
}

json g.render (dashboardAccess, [expand: expand])