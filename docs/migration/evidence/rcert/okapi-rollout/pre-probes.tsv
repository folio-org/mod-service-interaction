# Wire-capture probe list — read by capture.sh (review F-11 remediation).
# Tab-separated columns:
#   id	method	user	expect_legacy	expect_port	path	body
# - id: unique probe name (no spaces); becomes <id>.json in the capture dir.
# - user: A | B (X-Okapi-User-Id + tenant header) | notenant (tenant header deliberately omitted).
# - expect_legacy / expect_port: HTTP status capture.sh asserts for that side,
numgen	GET	A	200	200	/servint/numberGenerators?perPage=100&sort=code%3Basc	-
settings	GET	A	200	200	/servint/settings/appSettings?perPage=100&sort=key%3Basc	-
